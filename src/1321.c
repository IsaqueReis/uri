#include <stdio.h>
#include <stdbool.h>

bool jollo(int princesa[], int principe[])
{bool princesaGanha = true;
    if
    return princesaGanha;
}

int main(void)
{int a = 0, b = 0, c = 0, x = 0, y = 0;
    while(scanf("%d %d %d %d %d", &a, &b, &c,
          &x, &y) == 5 && a != 0 && b != 0 && 
                  c != 0 && x != 0 && y != 0)
    {
        
    }//while
        
    return 0;
}